export interface Profile {
    account_id: string,
    country: string,
    display_name: string,
    email: string,
    id: string,
    image: {
        height: number,
        url: string,
        width: number
    }
}
